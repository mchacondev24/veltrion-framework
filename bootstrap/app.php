<?php
/* VELTRION_PROTECTION_GUARD v1.0 | Commercial Build Protection */
if(!defined('VELTRION_RUNTIME_GUARD') && file_exists(__DIR__ . '/../bootstrap/guard.php')){ @include_once __DIR__ . '/../bootstrap/guard.php'; }
 require_once __DIR__ . '/helpers.php'; require_once __DIR__ . '/../vendor/autoload.php'; use Veltrion\Bootstrap\Container; $container = Container::getInstance(); $storageDirs = [ __DIR__ . '/../storage', __DIR__ . '/../storage/logs', __DIR__ . '/../storage/cache', __DIR__ . '/../storage/backups', __DIR__ . '/../storage/reports', __DIR__ . '/../storage/tests', ]; foreach ($storageDirs as $dir) { if (!file_exists($dir)) { mkdir($dir, 0777, true); } } $sqlitePath = __DIR__ . '/../storage/database.sqlite'; if (!file_exists($sqlitePath)) { touch($sqlitePath); } $container->singleton(\PDO::class, function () use ($sqlitePath) { $dbConfig = require __DIR__ . '/../config/database.php'; $driver = $dbConfig['default']; if ($driver === 'sqlite') { $pdo = new \PDO("sqlite:" . $sqlitePath); $pdo->setAttribute(\PDO::ATTR_ERRMODE, \PDO::ERRMODE_EXCEPTION); $pdo->setAttribute(\PDO::ATTR_DEFAULT_FETCH_MODE, \PDO::FETCH_ASSOC); return $pdo; } $cfg = $dbConfig['connections'][$driver] ?? $dbConfig['connections']['sqlite']; $dsn = "{$cfg['driver']}:host={$cfg['host']};port={$cfg['port']};dbname={$cfg['database']}"; $pdo = new \PDO($dsn, $cfg['username'], $cfg['password']); $pdo->setAttribute(\PDO::ATTR_ERRMODE, \PDO::ERRMODE_EXCEPTION); $pdo->setAttribute(\PDO::ATTR_DEFAULT_FETCH_MODE, \PDO::FETCH_ASSOC); return $pdo; }); return $container; 