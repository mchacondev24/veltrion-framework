<?php
namespace Veltrion\UseCases\Customer;

/* VELTRION_PROTECTION_GUARD v1.0 | Commercial Build Protection */
if(!defined('VELTRION_RUNTIME_GUARD') && file_exists(__DIR__ . '/../bootstrap/guard.php')){ @include_once __DIR__ . '/../bootstrap/guard.php'; }
  use InvalidArgumentException; use Veltrion\Domain\Entities\Customer; use Veltrion\Domain\Repositories\CustomerRepositoryInterface; use Veltrion\Domain\Repositories\UnitOfWorkInterface; use Veltrion\Domain\ValueObjects\Email; class CreateCustomerUseCase { private CustomerRepositoryInterface $customerRepository; private UnitOfWorkInterface $unitOfWork; public function __construct( CustomerRepositoryInterface $customerRepository, UnitOfWorkInterface $unitOfWork ) { $this->customerRepository = $customerRepository; $this->unitOfWork = $unitOfWork; } public function execute(array $data): array { $name = trim($data['name'] ?? ''); $emailStr = trim($data['email'] ?? ''); $phone = trim($data['phone'] ?? ''); if (empty($name)) { throw new InvalidArgumentException("El nombre del cliente es obligatorio."); } $email = new Email($emailStr); return $this->unitOfWork->execute(function () use ($name, $email, $phone) { $existing = $this->customerRepository->findByEmail($email->getValue()); if ($existing !== null) { throw new InvalidArgumentException("Ya existe un cliente registrado con el correo {$email->getValue()}"); } $customer = new Customer(null, $name, $email, $phone, 'ACTIVE'); $savedCustomer = $this->customerRepository->save($customer); return [ 'success' => true, 'message' => 'Cliente creado exitosamente.', 'data' => $savedCustomer->toArray(), ]; }); } } 